@*
    view imports
*@

@model List<Product>

@*
    view starts
*@

@foreach (var item in Model)
{
    <p>
    @item.Id - @item.Name - @($"{item.Price:c2}")
    </p>
}

@section Scripts{
    <script>
        App.name = "Kamil";
        console.log(App.welcome());
    </script>
}
